
---
name: compose-landing-page
description: 依据 user_config_info.json、业务规则、设计规范和可选既有页面结构，生成或迭代教育类落地页网站及其结构化页面蓝图。适用于新建或更新教育落地页、校验留资表单和约课规则、遵循教育转化 UI 标准，或输出可预览、可编辑、可发布的页面结构与网页产物；不用于无关网站、线上正式发布、第三方服务调用或非教育类页面。

---

# 教育落地页网站生成

## 目标

基于 user_config_info.json 标准化配置，产出一套满足转化规范、可实时预览、支持二次编辑、可交付发布的教育营销落地页完整页面方案。

## 必读输入

执行生成逻辑前，强制加载并校验四份基准文档：

1. user_config_info.json：用户配置的唯一事实来源。
2. references/business-rules.md：留资表单、约课流程和字段约束。
3. references/design-specifications.md：教育转化页面的视觉、信息层级和交互标准。
4. references/user-config-spec.md：配置字段含义和结构说明。
### 标准执行工作流
1. 配置解析与合法性校验
读取 user_config_info.json，对照 user-config-spec.md 校验字段格式、取值范围，拦截非法配置并生成校验提示。

2. **转化流程分层搭建（遵循业务规则 `business-rules.md`）**
根据配置开关区分转化链路，严格匹配约束条件：
- 关闭约课开关：仅搭建留资转化路径，页面不展示课时选择模块
- 开启约课开关：完整渲染体验课预约链路，包含课时筛选、时段选择组件
- 多子女模式开启：按配置上限渲染多子女信息录入模块，不超出最大子女数量限制
- 拓科展示：页面仅渲染配置文件内指定科目，禁止新增未配置科目
3. 页面视觉与结构渲染
依据 design-specifications.md 统一规范输出页面布局、文案层级、按钮、弹窗、表单交互样式，生成完整页面蓝图。

## 输出协议

最终返回一套可直接渲染、结构化完整的落地页页面产物（含页面蓝图、组件结构、表单配置、交互逻辑）。

